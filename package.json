{
  "name": "notes-app",
  "version": "1.0.0",
  "description": "Notion-like notes app with local storage",
  "scripts": {
    "start": "npx serve . -s -p 3000"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
