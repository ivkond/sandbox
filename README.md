# TODO List App

Простое TODO-приложение на Vite + React + TypeScript + Tailwind CSS + shadcn/ui

## Стек технологий

- ⚡ **Vite** - быстрый сборщик
- ⚛️ **React 18** - библиотека UI
- 📘 **TypeScript** - типизация
- 🎨 **Tailwind CSS** - стилизация
- 🧩 **shadcn/ui** - компоненты (Radix UI + Tailwind)
- 🐳 **Docker Compose** - контейнеризация

## Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

## Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

## Docker Compose

```bash
# Сборка и запуск
docker-compose up --build -d

# Остановка
docker-compose down
```

Приложение будет доступно по адресу: http://localhost:3000

## Структура проекта

```
todo-app/
├── src/
│   ├── components/
│   │   ├── ui/          # UI компоненты (Button, Input, Checkbox)
│   │   └── todo-list.tsx # Компонент списка задач
│   ├── lib/
│   │   └── utils.ts     # Утилиты (cn - class merge)
│   ├── App.tsx
│   └── index.css
├── Dockerfile
├── docker-compose.yml
├── nginx.conf
└── package.json
```

## Функционал

- ✅ Добавление задач (Enter или кнопка +)
- ✅ Отметка выполненных (чекбокс)
- ✅ Удаление задач (кнопка корзины)
- ✅ Счетчик задач
- 🌙 Поддержка темной темы
